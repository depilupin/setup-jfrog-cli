name: Safe PoC

on:
  issue_comment:
    types: [created]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate CLA Assistant injection
        run: |
          echo "Comment body received: $COMMENT"
        env:
          COMMENT: ${{ github.event.comment.body }}
